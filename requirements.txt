# Exact, tested pins -- this is what CI, the collector, Streamlit Cloud and a
# local `pip install -r requirements.txt` all install, so every environment
# runs the same versions (Python 3.12+; numpy 2.5.x has no 3.11 wheels).
#
# Deliberately the same pins as FPL-Analytics (same author, same toolchain):
# lightgbm==4.7.0, pandas==3.0.5, streamlit==1.63.0 etc. -- a change here
# should normally be mirrored there.
#
# Do not edit by hand: change requirements.in (the ranges) and re-resolve.
lightgbm==4.7.0
numpy==2.5.3
pandas==3.0.5
PuLP==3.3.2
pytest==8.4.2
pyarrow==25.0.1
scikit-learn==1.9.1
streamlit==1.63.0
